<%= form_with(model: response) do |form| %>
  <% if response.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(response.errors.count, "error") %> prohibited this response from being saved:</h2>

      <ul>
        <% response.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :prompt_id, style: "display: block" %>
    <%= form.text_field :prompt_id %>
  </div>

  <div>
    <%= form.label :discord_uid, style: "display: block" %>
    <%= form.text_field :discord_uid %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
